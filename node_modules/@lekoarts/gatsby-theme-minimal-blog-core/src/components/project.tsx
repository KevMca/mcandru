import React from "react"

export default function Project({ data, projectContext }: { data: any; projectContext: any }) {
  return (
    <div>
      <pre>{JSON.stringify(data, null, 2)}</pre>
      <pre>{JSON.stringify(projectContext, null, 2)}</pre>
    </div>
  )
}
